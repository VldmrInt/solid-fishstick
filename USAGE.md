# Инструкция по запуску Ozon Product Parser

## 📋 Требования

- Python 3.8+
- Google Chrome (последняя версия)
- Зависимости из `requirements.txt`

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Настройка config.json

Отредактируйте `config.json` и укажите URL магазина:

```json
{
  "seller_url": "https://www.ozon.ru/seller/your-shop-123456/?miniapp=seller_123456",
  "scroll_settings": {
    "scroll_pause": 2.0,
    "scroll_step_min": 500,
    "scroll_step_max": 800,
    "max_wait_seconds": 10,
    "max_scroll_attempts": 1000
  }
}
```

### 3. Запуск парсера

**Рекомендуемый способ (с видимым окном браузера):**

```bash
python run.py --method html --no-headless
```

**Headless режим (невидимый браузер):**

```bash
python run.py --method html
```

## ⚙️ Параметры запуска

| Параметр | Описание | Значение по умолчанию |
|----------|----------|----------------------|
| `--url` | URL магазина (переопределяет config.json) | Из config.json |
| `--method` | Метод парсинга: `api` или `html` | `html` |
| `--no-headless` | Показывать окно браузера | Скрыт |
| `--format` | Формат экспорта: `excel`, `xml`, `json`, `all` | `all` |
| `--output` | Имя файла без расширения | `seller_{ID}_{timestamp}` |
| `--max-pages` | Макс. страниц (для API метода) | 100 |

## 🔧 Настройка скорости скролла

Если Ozon не успевает загружать товары, отредактируйте `scroll_settings` в `config.json`:

```json
{
  "scroll_settings": {
    "scroll_pause": 3.0,          // Увеличьте паузу между скроллами (секунды)
    "scroll_step_min": 400,       // Уменьшите шаг скролла (пиксели)
    "scroll_step_max": 600,       // Уменьшите макс. шаг
    "max_wait_seconds": 15,       // Увеличьте таймаут без новых товаров
    "max_scroll_attempts": 2000   // Увеличьте лимит попыток для больших магазинов
  }
}
```

### Рекомендации по настройке:

- **Медленный интернет:** `scroll_pause: 3.0-4.0`, `max_wait_seconds: 15-20`
- **Большой магазин (1000+ товаров):** `max_scroll_attempts: 2000-3000`
- **Магазин быстро загружается:** `scroll_pause: 1.5`, `max_wait_seconds: 8`

## 📊 Что происходит при запуске

1. **Открытие страницы магазина** (~5 секунд)
2. **Проверка CloudFlare** (если есть, до 60 секунд)
3. **Бесконечный скролл:**
   - Скроллит страницу вниз
   - Ждет загрузки новых товаров (пауза из config.json)
   - Подсчитывает уникальные товары
   - Логирует прогресс каждые 20 итераций
   - Останавливается через N секунд без новых товаров
4. **Парсинг HTML** (извлечение данных)
5. **Фильтрация рекомендаций** (блок "Возможно, вам понравится")
6. **Экспорт в файлы** (Excel, XML, JSON)

## 📝 Примеры логов при правильной работе

```
2025-11-23 16:42:34 - ozon_parser - INFO - Запуск Ozon Product Parser
2025-11-23 16:42:34 - ozon_parser - INFO - Используется URL из config.json: https://...
2025-11-23 16:42:34 - ozon_parser - INFO - Метод парсинга: HTML
2025-11-23 16:42:34 - html_parser - INFO - Начало HTML парсинга магазина
2025-11-23 16:42:34 - html_parser - INFO - ℹ️  Ozon использует бесконечную ленту
2025-11-23 16:42:39 - html_parser - INFO - 📄 Загрузка страницы магазина...
2025-11-23 16:42:44 - html_parser - INFO - 🔄 Начинаем бесконечный скролл...
2025-11-23 16:42:44 - html_parser - INFO -    Настройки: пауза 2.0с, шаг 500-800px, таймаут 10с
2025-11-23 16:42:46 - html_parser - INFO -    Загружено товаров: 36 (+36)
2025-11-23 16:42:48 - html_parser - INFO -    Загружено товаров: 72 (+36)
2025-11-23 16:42:50 - html_parser - INFO -    Загружено товаров: 108 (+36)
...
2025-11-23 16:45:20 - html_parser - INFO -    Скролл #200: товаров 1850, времени без изменений: 3.2с
2025-11-23 16:45:30 - html_parser - INFO - ✅ Скролл завершен: 10 секунд без новых товаров
2025-11-23 16:45:30 - html_parser - INFO -    Всего загружено уникальных товаров: 1850
2025-11-23 16:45:30 - html_parser - INFO - Скролл завершен за 205 попыток
2025-11-23 16:45:31 - html_parser - INFO - 💾 HTML первой страницы сохранен в debug_html_page_1.html
2025-11-23 16:45:31 - html_parser - INFO - 🚫 Исключаем блок 'Возможно, вам понравится'
2025-11-23 16:45:32 - html_parser - INFO - ✅ Найдено товаров: 1823
2025-11-23 16:45:32 - ozon_parser - INFO - Успешно спарсено товаров: 1823
```

## ❗ Устранение проблем

### Проблема: Парсер останавливается слишком рано (мало товаров)

**Причина:** Озон не успевает загружать товары

**Решение:**
1. Увеличьте `scroll_pause` в config.json до 3.0-4.0 секунд
2. Увеличьте `max_wait_seconds` до 15-20 секунд
3. Используйте `--no-headless` для визуального контроля

### Проблема: Нет логов скролла ("🔄 Начинаем бесконечный скролл...")

**Причина:** Проблема с логированием (исправлена в последних коммитах)

**Решение:**
1. Убедитесь, что используете последнюю версию кода
2. Проверьте файл `src/utils/logger.py` - должна быть настройка root_logger

### Проблема: CloudFlare блокирует доступ

**Решение:**
1. Используйте `--no-headless` (видимое окно помогает обойти защиту)
2. Не запускайте парсер слишком часто (делайте паузы 5-10 минут)
3. Убедитесь, что Chrome обновлен до последней версии

### Проблема: Товаров в выгрузке меньше, чем в магазине

**Причина:** В магазине тысячи товаров, а парсер остановился раньше

**Решение:**
1. Увеличьте `scroll_pause` до 3.0-4.0 (дайте время на загрузку)
2. Увеличьте `max_wait_seconds` до 15-20 (некоторые товары подгружаются с задержкой)
3. Увеличьте `max_scroll_attempts` до 2000-3000
4. Используйте `--no-headless` и наблюдайте за процессом

### Проблема: Парсер включает рекомендованные товары

**Решение:**
- Это исправлено в последних коммитах
- Парсер автоматически исключает блок "Возможно, вам понравится"
- Проверьте логи на наличие строки: `🚫 Исключаем блок 'Возможно, вам понравится'`

## 📁 Результаты

После завершения файлы сохраняются в папку `output/`:

```
output/
├── seller_2860977_20251123_164234.xlsx  # Excel таблица
├── seller_2860977_20251123_164234.xml   # XML файл
└── seller_2860977_20251123_164234.json  # JSON файл
```

Для отладки также создается:
```
debug_html_page_1.html  # HTML код первой страницы
parser.log              # Полный лог работы
```

## 💡 Советы

1. **Первый запуск:** используйте `--no-headless` чтобы увидеть процесс
2. **Большие магазины:** увеличьте все таймауты в 1.5-2 раза
3. **Проблемы с загрузкой:** проверьте `debug_html_page_1.html` - там виден реальный HTML
4. **Повторный запуск:** делайте паузу 5-10 минут между запусками
5. **Оптимальные настройки:** `scroll_pause: 2.0`, `max_wait_seconds: 10-15`

## 🎯 Рекомендуемая команда запуска

```bash
python run.py --method html --no-headless
```

Эта команда:
- Использует HTML парсинг (надежнее API)
- Показывает окно браузера (помогает обойти CloudFlare)
- Использует настройки скролла из config.json
- Экспортирует во все форматы (Excel, XML, JSON)
